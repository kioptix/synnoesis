# --- Secrets / credentials (NEVER commit) ---
.env
.env.*
*.key
*.pem
*.crt
*.token
*token*.json
*-token.json
client_secret*.json
credentials.json
secrets.*
mqtt-passwd*
lan-api-key

# --- Runtime state / data (clone-local, NEVER commit) ---
state/
*.jsonl
*.log
*.out
*.bak*

# --- Internal design/tracking docs (NEVER publish) ---
# Working specs, build logs, roadmaps live here and stay OUT of the public repo.
# The published surface is code + user docs (docs/) -- not our working specs.
internal/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv*/
venv*/
.pytest_cache/

# --- Node ---
node_modules/
npm-debug.log*
yarn-error.log*

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
